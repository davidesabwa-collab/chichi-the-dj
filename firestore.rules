
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access for development and seeding.
    // WARNING: These are insecure rules and should be updated before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
